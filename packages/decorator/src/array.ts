import Any from './any';

export default class Array extends Any<Array> {
  max(val: number) {
    return Reflect.metadata(null, null);
  }
  min(val: number) {
    return Reflect.metadata(null, null);
  }
  length(val: number) {
    return Reflect.metadata(null, null);
  }
  unique(val: string) {
    return Reflect.metadata(null, null);
  }
}
